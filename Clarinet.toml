[project]
name = "token-revert"
authors = ["Token Security Team"]
description = "A secure token transaction reversal mechanism"
telemetry = false
requirements = []
[contracts.token-revert]
path = "contracts/token-revert.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
